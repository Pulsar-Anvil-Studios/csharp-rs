# csharp-rs

Generate C# type definitions from Rust structs and enums via `#[derive(CSharp)]`.

Respects `serde` attributes for JSON serialization compatibility, making it ideal for sharing types between a Rust backend and a C#/.NET or Unity client.

## Status

Work in progress.
